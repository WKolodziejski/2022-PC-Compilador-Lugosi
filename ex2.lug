main {
    var int x;
    x := fatorial (10);

    if ((x > 0)) {
        print (x);
    };
}

function int fatorial(int x) {
    if (((x == 0) || (x == 1))) {
        return 1;
    };

    return (x * fatorial ((x - 1)));
}